import {IconProps as PhospherIconProps} from "@phosphor-icons/react"

export type { Icon as PhospherIconComp } from "@phosphor-icons/react"

export interface IconProps extends PhospherIconProps {
  name: string;
}
  
